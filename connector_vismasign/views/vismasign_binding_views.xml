<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!-- Tree view -->
    <record id="view_vismasign_binding_tree" model="ir.ui.view">
        <field name="name">vismasign.binding.tree</field>
        <field name="model">vismasign.binding</field>
        <field name="arch" type="xml">
            <tree>
                <field name="create_date" />
                <field name="backend_id" />
                <field name="method" />
                <field name="endpoint" />
                <field name="status_code" />
                <field name="successful" />
            </tree>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_vismasign_binding_form" model="ir.ui.view">
        <field name="name">vismasign.binding.form</field>
        <field name="model">vismasign.binding</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="backend_id" />
                        <field name="method" />
                        <field name="endpoint" />
                        <field name="status_code" />
                        <field name="successful" />
                    </group>
                    <notebook>
                        <page name="payload" string="Payload">
                            <group>
                                <field name="payload" widget="code" />
                            </group>
                        </page>
                        <page name="response" string="Response">
                            <group>
                                <field name="response" widget="code" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_vismasign_binding" model="ir.actions.act_window">
        <field name="name">Visma Sign Bindings</field>
        <field name="res_model">vismasign.binding</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Menu under Visma Sign root -->
    <menuitem
        id="menu_vismasign_binding"
        name="Bindings"
        parent="menu_vismasign_root"
        action="action_vismasign_binding"
    />
</odoo>
